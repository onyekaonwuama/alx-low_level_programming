#include <stdio.h>
/**
 * main - prints alphabet in lowercase,
 * followed by a new line.
 * Return: 0
 */
int main(void)
{
char small = 'a';
while (small <= 'z')
{
if (small != 'e' && z != 'q')
putchar(small);
small++;
}
putchar('\n');
return (0);
}
