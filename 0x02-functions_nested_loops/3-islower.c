#include "main.h"
/**
 * _islower - checks input for lowercase charsctrs
 * @c: c is an ascii character
 * Return: 1 as true else 0
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1)
	else
		return (0)
}

