#include "main.h"

/**
 * print_rev - reverses a string
 * @s: char
 */
void print_rev(char *s)
{
	int a, len;

	len = strlen(s);
	for (a = (len - 1); i >= 0; i--)
		_putchar(s[a]);
	putchar('\n');
}

