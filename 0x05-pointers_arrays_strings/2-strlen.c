#include "main.h"
#include <stdio.h>
/**
*_strlen - return the length of a string
*@s: strig given
*Return: len of str
*/
int _strlen(char *s)
{
int a;
for (a = 0; s[a] != '\0'; a++)
{
}
return (a);
}
